const enum PolymorphicTables {
    requests = 'requests'
};

const enum APPLY_REACTION {
    IS_ACCEPT_UNTREATED = 0, //未処理
    IS_ACCEPT_ARROW = 1, //OK
    IS_ACCEPT_DENY = 2 //NO
};

const enum APPLY_SENDER_NOTICE {
    IS_NOTIFIED_YET = 1, //未送信
    IS_NOTIFIED_DONE = 2 //送信済
};

